using UnityEngine;

public class MenuPrincipal : MonoBehaviour // MonoBehaviour veut dire script dans unity en gros
{
    public void quitButon()
    {
        Application.Quit();
        // Fonction appeler lorsque que l'on appuie sur Quitter
    }
    // Le code du bouton option est directement dans fait apartir de l'éditeur unity
}